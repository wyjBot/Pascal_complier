program example(input,output);
    const pi = 3.14;
          e=2.73;
    var x,y:integer;
        z:array [3..9] of char;
    function gcd(a,b:integer):integer;
        begin 
            if b=0 then gcd:=a
            else gcd:=gcd(b, a mod b)
        end;
    begin
        z[4] := 1;
        read(x, y);
        write(gcd(x, y))
    end.