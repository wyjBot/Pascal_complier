#include<stdbool.h>
#include<stdio.h>

float x, y; 
float z[3][4]; 

int main(int argc, char* argv[]){
	z[1.2 + 2-1][0] = 3; 
	x = 1; 
	y = 2; 
	scanf("%f%f", &x, &y); 
	for(x = 1; x < 5; x++){
		printf("%f\n", 3.14); 
	}
	printf("%f\n", x); 
}
