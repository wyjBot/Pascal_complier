Program example(input, output);
const pi=-3.14;
Var x, y : real;
    z: array[1..3, 2..5] of real;
Function gcd(a, b : integer) : integer;
Begin
  If b = 0 Then gcd := a
  Else gcd := gcd(b, a Mod b)
End;
Begin
  z[1.2 + 2,2] := 3;
  x := 1;
  y := 2;
  read(x, y);
  for x := 1 to 5
  do
    write(3.14);
  write(gcd(x, y))
End.